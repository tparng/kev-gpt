# Normalize line endings: LF in the repo for all text files.
# Prevents OneDrive/Windows syncs from flipping the whole tree to CRLF.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.bit binary
*.bit.bin binary
*.mem -text
