# Core: part-of-speech tagging and lemmatisation.
spacy>=3.7
# The small English model is loaded by name; install it after pip install:
#   python -m spacy download en_core_web_sm

# Optional: GPT-2 BPE proxy for token-count compression in the harness.
# The harness runs without it and reports word counts only.
tiktoken>=0.5

# Optional: Brevitas INT4 QAT path (model/qgpt.py). Only needed for
# `python -m model.train --qat`; the FP path runs without it.
# brevitas>=0.11

# Dev/test only.
pytest>=7.0

# Genesys2 board tooling: sends the weight image over the UART loader
# (fabric/genesys2/send_weights.py). Only needed for real-hardware bring-up.
pyserial>=3.5
